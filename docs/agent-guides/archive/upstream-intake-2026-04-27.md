# Upstream Intake Tracker

Date: 2026-04-27
Status: Current intake batch validated; remaining items deferred or skipped

## Purpose

Track upstream OpenClaw changes that may be worth selectively porting into this fork.

This is not an implementation plan for a single PR. It is the running ledger so agents can see:

- what has been identified
- what is next
- what is in progress
- what has landed
- what was intentionally skipped

For future loose signals that are not ready to become intake work, use `docs/agent-guides/upstream-updates-inbox.md`.

## Guardrails

- Do not merge `upstream/main`.
- Port selectively from GitHub/upstream diff evidence.
- Treat Twitter/bookmarks/social posts as hints only. Confirm every real candidate against GitHub upstream commits or PRs before adding it to the port plan.
- Keep consumer-product behavior protected:
  - Mac-first local operator
  - Telegram-first user experience
  - curated Codex/OpenAI model defaults
  - isolated consumer runtime, profile, state, config, port, and LaunchAgent behavior
- Do not pull broad upstream platform expansion unless a concrete consumer need justifies it.
- Mark each item complete only after the port lands and validation evidence is recorded.

## Status Legend

- `candidate`: worth tracking, no port work started
- `planned`: selected for a near-term port slice
- `in-progress`: implementation lane is active
- `ported`: landed in this fork
- `validated`: landed and behavior was proved with tests or live checks
- `deferred`: useful, but not now
- `skipped`: intentionally not porting

## Current Recommendation

Current recommendation: stop this intake batch.

Why: the highest-value May 2026 slices landed and the live runtime is healthy. The remaining candidates are either broad plugin/runtime rewrites, low-urgency metadata, or architectural expansion that should wait for a concrete product symptom.

If a new symptom appears, the next review target is `#76079` plugin tool descriptor caching because it could reduce prompt-time plugin loading. Do not port it without measuring startup/reply latency first.

The final Twitter/bookmark audit found real missed upstream candidates in the `v2026.4.29` to `v2026.5.2` window. Keep social/bookmark review as a final hint pass for future intake cycles, but continue treating GitHub PRs, commits, and release diffs as the source of truth.

## Intake Workflow

1. Use GitHub/upstream commits and PRs as the source of truth.
2. Rank candidates by consumer-product leverage.
3. Port and validate the highest-leverage slices first.
4. Update the completed log as each slice lands.
5. Final audit: review Twitter/bookmarks/social hints for potentially missed upstream changes.
6. If the final audit finds no missed high-value changes, stop using Twitter bookmarks for future intake cycles.
7. If the final audit does find missed high-value changes, keep the bookmark audit as a recurring last-step check.

## Intake Plan

| Rank | Status    | Upstream change                                                        | What changed                                                                                                                         | Upstream files                                                                                                                                                                                              | Why it matters here                                                                                      | Recommendation | First porting slice                                                                                                                                                             | Validation notes                                                         |
| ---- | --------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 1    | deferred  | `#71985` `fix(models): preserve provider index catalog fallback`       | Keeps model listing sane when provider index data exists but catalog rows are incomplete or stale.                                   | `src/commands/models/list.*`, `src/model-catalog/**`                                                                                                                                                        | Fork recently changed GPT-5.5/Codex defaults. Bad model lists break onboarding and picker trust.         | defer          | Upstream provider-index/catalog layout does not exist in this fork. Reassess only if fork adopts that model-catalog path.                                                       | No local port target found on 2026-05-02                                 |
| 2    | deferred  | `#71980` `fix(codex): translate --thinking minimal to low`             | Maps legacy `minimal` thinking setting to modern Codex `low`.                                                                        | `extensions/codex/provider.ts`, `extensions/codex/src/app-server/thread-lifecycle.ts`, test                                                                                                                 | Small compatibility fix for modern Codex models.                                                         | defer          | Upstream `extensions/codex` path does not exist in this fork. Reassess against local reasoning plumbing separately.                                                             | No direct local port target found on 2026-05-02                          |
| 3    | validated | `#71933`, `90801ba400`, `f2fdb9d125` model auth/discovery truthfulness | Model auth writes respect selected agent; OpenAI/Codex transport metadata is normalized; provider IDs are normalized in auth status. | `src/commands/models/auth.ts`, `src/cli/models-cli.ts`, `extensions/openai/*`, `src/agents/pi-model-discovery.ts`, `src/gateway/server-methods/models-auth-status.ts`                                       | Prevents "available but broken" model states during setup and runtime.                                   | ported         | Ported agent-scoped model auth and OpenAI Codex stale metadata/discovery normalization. Gateway `models-auth-status` path does not exist locally.                               | Targeted tests passed 2026-05-02                                         |
| 4    | validated | `#71825`, `#71952` Telegram reply/progress UX                          | Preserves native quote replies and keeps verbose/tool progress fallback working.                                                     | `extensions/telegram/src/bot-message-dispatch.ts`, `extensions/telegram/src/bot-message-context.session.ts`, `extensions/telegram/src/bot/delivery.*`, `extensions/telegram/src/reply-parameters.ts`, tests | Telegram is the primary interface. Broken replies and missing progress make the product feel unreliable. | ported         | Ported native quote metadata propagation, quote fallback retry, and fork-equivalent tool progress fallback coverage.                                                            | Targeted tests passed 2026-05-02                                         |
| 5    | validated | `78df859e15`, `#69288` visible assistant text cleanup                  | Strips raw `<function>` tags and Anthropic/ANTML thinking tags from visible assistant text and streams.                              | `src/shared/text/assistant-visible-text.ts`, `src/agents/pi-embedded-subscribe.ts`, `src/agents/pi-embedded-utils.ts`, tests                                                                                | Tool syntax leaks damage ChatGPT/personality quality in Telegram.                                        | ported         | Ported standalone function-call block stripping and `antml:thinking` support.                                                                                                   | Targeted tests passed 2026-05-02                                         |
| 6    | validated | `ecfaf64526` host tilde path handling                                  | Aligns `~` expansion with actual OS home.                                                                                            | `src/agents/pi-tools.read.ts`, `src/agents/pi-tools.host-edit.ts`, tests                                                                                                                                    | A Mac operator must not stumble on basic local file paths.                                               | ported         | Ported host read/write/edit/recovery `~` expansion against OS home instead of `OPENCLAW_HOME`.                                                                                  | Targeted tests passed 2026-05-02                                         |
| 7    | validated | `4f00b76925`, `4bc46ccfed` context and compaction hardening            | Tightens context limits, bounds tool results, and caps compaction reserve for small models.                                          | `src/agents/agent-scope*`, `src/agents/pi-embedded-runner/**`, `src/agents/session-tool-result-guard*`, memory files                                                                                        | Long personal-agent sessions degrade without good context hygiene.                                       | ported         | Ported the safe local subset: per-agent tool-result caps, persisted tool-result truncation, and small-context compaction reserve caps. Deferred broad memory-core/config churn. | Targeted tests passed 2026-05-03; `tsgo` still has unrelated repo errors |
| 8    | validated | `77719899f3`, `4ee537a04a` gateway/node-host recovery                  | Refreshes stale embedded service tokens and keeps node-host recovering after gateway restarts.                                       | `src/cli/daemon-cli/install.ts`, `src/gateway/client.ts`, `src/node-host/runner.ts`, tests                                                                                                                  | Relevant to macOS runtime reliability, but must respect consumer runtime isolation.                      | ported         | Ported node-host reconnect recovery and gateway install stale-token refresh. Preserved runtime identity and shared-service ownership guards.                                    | Targeted tests passed 2026-05-03: 3 files, 44 tests                      |
| 9    | triaged   | `#71997` runtime dependency install hardening                          | Hardens dependency install surfaces across Signal, skills, bundled plugins, watch-node, Docker/package paths.                        | `extensions/signal/src/client.ts`, `scripts/watch-node.mjs`, `src/agents/skills-install.ts`, `src/channels/plugins/bundled.ts`, `package.json`, `src/dockerfile.test.ts`, tests                             | Broad preventative reliability work, but low immediate value for a Telegram-first Mac fork.              | defer/skip now | Do not port as a bundle. Revisit only the npm skill-install prefix if non-root Docker/runtime skill installs become a real product path.                                        | Triage complete 2026-05-03                                               |
| 10   | skipped   | Android/iOS/Matrix/Teams/WhatsApp/dashboard-heavy UX/TTS expansion     | Broad platform and channel expansion.                                                                                                | Many upstream platform/channel files                                                                                                                                                                        | Consumer fork is Telegram-first, Mac-first, and intentionally minimal.                                   | skip           | Revisit only if a concrete beta-user need appears.                                                                                                                              | Not applicable                                                           |

## Completed Intake Log

| Date       | Status    | Item                                     | Fork commit / PR       | Validation                                                                                                     | Notes                                                                                                                                                                   |
| ---------- | --------- | ---------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-02 | validated | Model auth/discovery truthfulness subset | `#591`, `6f6a59ace4`   | `pnpm exec vitest run ...` aggregate targeted suite: 13 files, 253 tests passed                                | Ported agent-scoped auth commands and OpenAI Codex metadata/discovery normalization. `#71985` and gateway auth-status pieces had no direct local target.                |
| 2026-05-02 | validated | Telegram reply/progress UX               | `#591`, `6f6a59ace4`   | Aggregate targeted suite: 13 files, 253 tests passed                                                           | Ported native quote reply propagation, quote rejection fallback, and fork-equivalent progress fallback coverage.                                                        |
| 2026-05-02 | validated | Visible assistant text cleanup           | `#591`, `6f6a59ace4`   | Aggregate targeted suite: 13 files, 253 tests passed                                                           | Ported standalone function tool-call stripping and namespaced `antml:thinking` stripping/promoting.                                                                     |
| 2026-05-02 | validated | Host tilde path handling                 | `#591`, `6f6a59ace4`   | Aggregate targeted suite: 13 files, 253 tests passed                                                           | Ported OS-home tilde expansion for host read/write/edit and edit recovery.                                                                                              |
| 2026-05-03 | validated | Context/compaction hardening             | `#593`, `bca9fabd3e`   | Targeted suite: 5 files, 85 tests passed; `git diff --check` passed                                            | Ported per-agent tool-result caps, persisted tool-result truncation, and small-context compaction reserve caps.                                                         |
| 2026-05-03 | validated | Gateway/node-host recovery               | `#595`, `a207685809`   | Targeted suite: 3 files, 44 tests passed; `git diff --check` passed                                            | Ported managed gateway reconnect timers, node-host auth-pause supervisor exits, and stale embedded gateway-token install refresh.                                       |
| 2026-05-04 | validated | Telegram/thread visible-reply slice      | `#600`, `22533e2fe4`   | Worker targeted suites passed; coordinator spot-check: 3 files, 70 tests passed                                | Ported unified `threadBindings.spawnSessions`, group/native visible-reply preservation, and message-tool unavailable fallback. Runtime and channel probes later passed. |
| 2026-05-04 | validated | Codex/model truthfulness slice           | `#601`, `4fad18b2b8`   | Worker stream-wrapper/OpenAI targeted tests passed                                                             | Ported stale Codex mini suppression and wrapped Codex OAuth stream preservation.                                                                                        |
| 2026-05-04 | validated | Startup secrets fast path                | `#604`, `b8637984ea`   | Targeted suites passed: startup secrets 7 tests, auth-profile readonly sync 3 tests; `git diff --check` passed | Ported lean startup/restart-check preflight auth-store loading while keeping startup activation, reload, and auth bootstrap overlay-capable.                            |
| 2026-05-06 | validated | Deploy/runtime validation fixes          | `#608`, `#609`, `#610` | `/healthz` live; gateway RPC ok; `channels status --probe` passed                                              | Restored main-built app parity, restart abort behavior, and Bash 3 Sparkle packaging compatibility needed to deploy the intake batch cleanly.                           |

## Active Work Log

Use this section for short handoff notes while a port is in progress.

| Date       | Item                                | Owner / lane      | State     | Notes                                                                                                                                                                    |
| ---------- | ----------------------------------- | ----------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-04-27 | Intake tracker created              | Codex             | active    | Seeded from Apr 26 upstream research. No porting work started.                                                                                                           |
| 2026-05-02 | First intake implementation wave    | Codex + workers   | validated | Ported model/auth subset, Telegram replies/progress, visible-text cleanup, and host tilde paths. Deferred upstream paths that do not exist in this fork.                 |
| 2026-05-03 | Context/compaction hardening slice  | Codex + worker    | validated | Ported the smallest safe subset from `4f00b76925` and `4bc46ccfed`; targeted tests passed locally. Skipped memory-core excerpt bounds and generated config/schema churn. |
| 2026-05-03 | Gateway/node-host recovery mapping  | Codex + worker    | mapped    | Read-only mapping found both upstream fixes are worth porting manually. Direct cherry-pick rejected because fork runtime identity and browser-proxy behavior diverge.    |
| 2026-05-03 | Gateway/node-host recovery port     | Codex + workers   | validated | Ported node-host reconnect recovery and gateway install stale-token refresh; targeted tests passed locally.                                                              |
| 2026-05-03 | Runtime dependency hardening triage | Codex             | triaged   | `#71997` is not worth porting as a bundle. Signal/watch-node/package pieces are low-value here; npm skill-install prefix is the only possible later slice.               |
| 2026-05-04 | Final bookmark audit                | Codex + explorers | mapped    | Twitter/X bookmark hints found real missed upstream changes in `v2026.4.29` to `v2026.5.2`; GitHub diff review below is the actionable source of truth.                  |
| 2026-05-04 | Telegram/thread visible-reply port  | Worker            | validated | Merged as `#600` / `22533e2fe4`; focused worker tests and coordinator spot-check passed. Live runtime validated after the deploy follow-up.                              |
| 2026-05-04 | Codex/model truthfulness port       | Worker            | validated | Merged as `#601` / `4fad18b2b8`; stream-wrapper and OpenAI tests passed. Broader model/catalog tests were not needed for the local subset.                               |
| 2026-05-04 | Startup secrets fast path           | Worker            | validated | Merged as `#604` / `b8637984e`; targeted startup-secret tests passed.                                                                                                    |
| 2026-05-06 | Intake deploy/runtime smoke         | Codex + worker    | validated | Sacred main fast-forwarded to `c28cab2b6e`; `/healthz` returned live, gateway RPC passed, and configured Telegram channel probes worked.                                 |

## May 2026 Bookmark Audit

Source window:

- Upstream `v2026.4.29`: release published 2026-04-30.
- Upstream `v2026.5.2`: tag `d07e13380c24b131d6a9260fbb6a28a35e6241e7`, release published 2026-05-02.
- Fork `origin/main`: `c28cab2b6e` on 2026-05-06 after PR `#610`.

| Rank | Status    | Upstream change                                                                   | What changed                                                                                                                                   | Upstream files                                                                                                                                                                                                            | Why it matters here                                                                                                                               | Recommendation | First porting slice                                                                                                                       |
| ---- | --------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 11   | validated | `#75943` thread-bound spawn config                                                | Replaces split `threadBindings.spawnSubagentSessions` / `spawnAcpSessions` toggles with one `threadBindings.spawnSessions` flag and migration. | `src/channels/thread-bindings-policy.ts`, `src/config/types.base.ts`, `src/config/zod-schema.providers-core.ts`, `docs/tools/acp-agents.md`, `extensions/telegram/src/config-ui-hints.ts`, `src/agents/acp-spawn.test.ts` | Fork still exposes the old split toggles. This directly affects Telegram topic-bound ACP/subagent behavior and removes confusing config.          | ported         | Merged as `#600` / `22533e2fe4`; worker tests and coordinator spot-check passed. Runtime and Telegram channel probes passed after deploy. |
| 12   | validated | `#73672`, `#74877` group visible-reply delivery                                   | Keeps native slash-command replies visible in groups and falls back to automatic source delivery when message-tool-only reply mode cannot run. | `src/auto-reply/command-auth.ts`, `src/auto-reply/reply/source-reply-delivery-mode.ts`, `src/auto-reply/reply/dispatch-from-config.ts`, `src/plugin-sdk/channel-reply-pipeline.ts`, tests                                 | Telegram/group replies are core UX. The fork is missing the upstream delivery-mode helper, so silent or hidden replies remain possible.           | ported         | Merged as `#600` / `22533e2fe4`; focused worker tests and coordinator spot-check passed. Runtime and channel probes passed after deploy.  |
| 13   | validated | `#74451` stale Codex mini model suppression                                       | Prevents stale `openai-codex/gpt-5.4-mini` configured rows from bypassing provider capability checks and breaking ChatGPT-backed Codex runs.   | `extensions/openai/openclaw.plugin.json`, `extensions/openai/openai-codex-provider.ts`, `src/agents/pi-embedded-runner/model.ts`, tests                                                                                   | This fork cares about a small truthful model picker. A stale configured Codex row is exactly the kind of "available but broken" lie users notice. | ported         | Merged as `#601` / `4fad18b2b8`; stream-wrapper and OpenAI tests passed.                                                                  |
| 14   | validated | `#75111` Codex OAuth stream attribution                                           | Preserves already wrapped Codex streams during OpenAI attribution and strips only native-backend-only payload fields.                          | `extensions/openai/openai-codex-provider.ts`, `src/agents/pi-embedded-runner/openai-stream-wrappers.ts`, `src/agents/openai-transport-stream.ts`, tests                                                                   | Same local seam exists. Valuable if Codex OAuth sessions still risk missing bearer injection or stream wrapper replacement.                       | ported         | Merged as `#601` / `4fad18b2b8`; stream-wrapper regression passed in worker lane and coordinator spot-check.                              |
| 15   | validated | `#68327` startup secrets fast path                                                | Skips plugin-backed auth-profile overlays during startup secrets preflight while keeping reload/OAuth recovery overlay-capable.                | `src/gateway/server-startup-config.ts`, `src/gateway/server-startup-config.secrets.test.ts`                                                                                                                               | Mac packaged startup latency is product-critical. This is a focused fast-path, not a broad plugin-system rewrite.                                 | ported         | Merged as `#604` / `b8637984e`; targeted startup secret tests passed and runtime later validated live.                                    |
| 16   | deferred  | `#76079` plugin tool descriptor cache                                             | Caches plugin tool descriptors so prompt-time planning can avoid loading plugin runtimes repeatedly.                                           | `src/plugins/tools.ts`, `src/plugins/tool-descriptor-cache.ts`, `src/plugins/tools.optional.test.ts`, docs                                                                                                                | Could improve reply startup and reduce plugin-load pressure, but touches shared plugin planning.                                                  | later          | Reassess only if startup/reply latency is measured. Do not port as part of this intake batch.                                             |
| 17   | deferred  | `#63244` `spawnedBy` event metadata                                               | Adds parent-session metadata to chat and agent broadcast payloads.                                                                             | `src/gateway/server-chat.ts`, `src/gateway/protocol/schema/agent.ts`, `src/gateway/protocol/schema/logs-chat.ts`, protocol models, tests                                                                                  | Low-risk additive metadata, useful for clients routing subagent events, but not urgent unless live clients need it now.                           | later          | Add `spawnedBy` to gateway event payloads and regenerate protocol models.                                                                 |
| 18   | deferred  | `#75360`, `#75114` Telegram cold-start/networking reliability                     | Keeps typing/tool warmup off the reply critical path and improves Telegram startup/fallback transport handling.                                | `src/auto-reply/reply/typing.ts`, `src/channels/typing.ts`, `extensions/telegram/src/bot-core.ts`, `extensions/telegram/src/fetch.ts`, tests                                                                              | Real reliability work, but lower ROI than visible reply/thread-binding correctness unless latency or reconnect churn is observed.                 | later          | Only port after a measured Telegram latency/reconnect symptom; start with focused fetch/typing tests.                                     |
| 19   | deferred  | `#74189` inferred follow-up commitments                                           | Adds hidden follow-up extraction, storage, CLI, and heartbeat-delivered reminders.                                                             | `src/commitments/**`, `src/infra/heartbeat-runner.ts`, `src/commands/commitments.ts`, config/schema/docs                                                                                                                  | Potentially useful, but it adds autonomous behavior. Risk of noisy or surprising product behavior is high.                                        | later          | Design an explicit opt-in product policy before porting any code.                                                                         |
| 20   | deferred  | `#75183` runtime dependency repair cluster                                        | Reworks bundled plugin dependency install, repair, manifest, doctor, and reporting paths.                                                      | `src/commands/doctor-bundled-plugin-runtime-deps.ts`, `src/plugins/bundled-runtime-deps*.ts`, `src/plugins/loader.ts`, `src/cli/plugins-*`, tests                                                                         | High value for packaged installs, but too structural to port as a bundle; several helper surfaces are absent locally.                             | later          | Revisit one leaf at a time only after a concrete packaged-runtime dependency failure.                                                     |
| 21   | skipped   | Native Codex harness expansion `#75308`, `#75765`, status `#76197`, Codex `/goal` | Upstream improves native Codex harness tools, message-tool reply defaults, heartbeat replies, status, and external Codex goal workflows.       | `extensions/codex/**`, `docs/plugins/codex-harness.md`, `src/status/status-text.ts`, Codex upstream PRs                                                                                                                   | This fork does not have upstream `extensions/codex/**`; importing the harness path now would be architectural scope creep.                        | skip now       | Reassess only if this fork adopts the native Codex harness path.                                                                          |

Already local from earlier upstream pulls, so do not re-port:

- `#57548` Telegram native command session metadata.
- `#75073` Telegram durable streaming edits.
- `#75382` visible reply path work.

## Source Snapshot

- Upstream source of truth checked against `openclaw/openclaw`.
- Upstream `main`: `9089e6b595` on 2026-04-26.
- Fork `origin/main`: `c28cab2b6e` on 2026-05-06.
- Research focus:
  - ChatGPT/personality/prompt behavior
  - model support/model picker changes
  - tool/runtime reliability improvements
  - UX/app packaging changes
